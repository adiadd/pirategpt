{
	"name": "server",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --remote",
		"start": "wrangler dev --remote",
		"dev:exp": "wrangler dev --experimental-vectorize-bind-to-prod --remote",
		"start:exp": "wrangler dev --experimental-vectorize-bind-to-prod --remote",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20250224.0",
		"typescript": "^5.5.2",
		"wrangler": "^3.111.0"
	},
	"dependencies": {
		"@google/generative-ai": "^0.24.0",
		"hono": "^4.7.2"
	}
}